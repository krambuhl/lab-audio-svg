{
  "name": "heptagon",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "copy": "rsync -r source/static/ dist",
    "dev": "yarn run copy && parcel source/index.html",
    "build": "yarn run copy && parcel build source/index.html"
  },
  "devDependencies": {
    "parcel-bundler": "^1.7.0",
    "postcss-cssnext": "^3.1.0",
    "postcss-nested": "^3.0.0"
  },
  "dependencies": {
    "audio-generator": "^2.1.2",
    "audio-speaker": "^1.5.0",
    "mtof": "^1.0.2",
    "nanobus": "^4.3.3",
    "normalize.css": "^8.0.0"
  }
}
